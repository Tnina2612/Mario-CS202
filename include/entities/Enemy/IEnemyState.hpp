#pragma once

class Enemy;

class IEnemyState {
public:
    // virtual ~IEnemyState() {}
    // virtual void enter(Enemy& enemy) = 0;
    // virtual void update(Enemy& enemy) = 0;
    // virtual void exit(Enemy& enemy) = 0;
    // virtual void execute(Enemy& enemy) = 0;
    /*
    virtual void onStomp(Enemy& enemy, Player& player) = 0; 
    virtual void onTouch(Enemy& enemy, Player& player) = 0;
    */
};